---
skip_list:
  - no-handler

warn_list:
  - experimental

kinds:
  - playbook: "**/examples/*.{yml,yaml}"
  - tasks: "**/tasks/*.{yml,yaml}"
  - vars: "**/vars/*.{yml,yaml}"
  - meta: "**/meta/main.yml"
  - yaml: "**/*.{yml,yaml}"

verbosity: 1
