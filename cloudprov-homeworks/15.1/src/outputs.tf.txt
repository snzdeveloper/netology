
output "totals" {
    #{ for k,v in [yandex_compute_instance.storage]: k => "${v.network_interface.0.nat_ip_address}" }
  value = [ 
    for v in flatten([ yandex_compute_instance.public_vm, [ for k,v in yandex_compute_instance.private_vm: v ], yandex_compute_instance.nat ]): {
      name = "${v.name}"
      id = "${v.id}"
      fqdn = "${v.fqdn}"
    } 
  ]
}
